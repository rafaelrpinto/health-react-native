import React, {Component} from 'react';
import {Text} from 'react-native-elements';

/**
 * Container for gps location search.
 */
export default class LocationSearch extends Component {

  /**
   * Constructor.
   */
  constructor(props) {
    super(props);
    this.state = {}
  }

  render() {
    return <Text>Location Search</Text>;
  }
}
